stages:
  - build

build:
  stage: build
  variables:
    MAVEN_OPTS: >-
      -Dhttps.protocols=TLSv1.2
      -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository
      -Dorg.slf4j.simpleLogger.showDateTime=true
      -Djava.awt.headless=true
    JAVA_HOME: /srv/jdk8u492-b09
  script:
    - echo $JAVA_HOME
    - mvn -V -B clean package
    - mvn -V -B compile jib:build -Dto.auth.username=$JIB_TO_AUTH_USERNAME -Dto.auth.password=$JIB_TO_AUTH_PASSWORD -DCI_PIPELINE_IID=$CI_PIPELINE_IID
    - mvn -V -B compile jib:build -Dto.auth.username=$JIB_TO_AUTH_USERNAME -Dto.auth.password=$JIB_TO_AUTH_PASSWORD -DCI_PIPELINE_IID=$CI_PIPELINE_IID -P loong64
  cache:
    key: "${CI_JOB_NAME}-${CI_COMMIT_BRANCH}"
    paths:
      - .m2/repository
  tags:
    - shell
